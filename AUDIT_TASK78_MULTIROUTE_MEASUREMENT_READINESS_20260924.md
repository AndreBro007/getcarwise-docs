# Task #78 — Read-only multi-route measurement and routing audit
**24 September 2026. Status:** completed as a desk audit by ChatGPT. No synthetic visit, app invocation, API/affiliate click, ad account, dashboard setting, code deployment or live edit was made. This does **not** certify live analytics collection or a source-to-approved-lead chain.

## 1. Scope and evidence

Inspected targeted current GitHub files and the existing verified administration records rather than replaying owner tests. `carclever-widget` main was at `0158ba80465dc255e431871802225fbfe99a16c4`; the standalone Lite V2 code is on `feature/carclever-lite-v2`; the Find My Car repo default and `release/v2` still have CJ link code, while `edmunds-impact-swap` contains the Impact link wrapper. STATE.md records live aliases verified at commit `dd68e15` on September 17, so default-branch CJ code is **not** evidence that the live submitted app reverted. No fresh alias/version test was run during the quiet period.

Read-only records: Task #63A validated assets New 3949597, Used 3949600, Trade-in 3949601; page 934's published Used href `https://edmunds.sjv.io/c/7765200/3949600/52125` was checked without a partner click on September 24. The prior Task #72 live test found MonsterInsights Lite produced **zero outbound-click events**; page 1160 then received three Impact UI-generated Sub ID links (`page1160-new`, `page1160-used`, `page1160-tradein`) and their resolution was tested then. The GA4–Search Console linking flow was performed September 23, but stream collection from both hosts is not verified. Impact screenshots show 18 recent clicks/0 actions, with 17 “no referring domain”; their origin is unknown.

The old live Fractal-hosted @CarClever code and current Impact/GA4 account reports were not available in this GitHub desk pass. Do not infer its internal instrumentation, source propagation, or present-day tool volume.

## 2. Route-by-route observability and decisions

| Route | Observed now | Break / unverified point | Honest first outcome |
|---|---|---|---|
| G1 Google → used page 934 → app or Used asset | Existing used-only page and correct untagged Impact Used href verified previously. WordPress has MonsterInsights GA4 page tracking; Lite outbound tracking was tested absent. | Google paid session and consented collection not yet checked in GA4. Page 934 Used link lacks an observed route/campaign-specific Impact Sub ID. App-choice click and ensuing app use unverified. Impact report cannot be joined to a specific paid cohort merely from aggregate clicks. | Search spend/visits can be read once launched; **do not call this a source-specific approved-lead test until Sub ID/action reporting is verified.** |
| G2 New / Trade-in → page 828 or Decision Center 1160 | Task #63A mapped three assets. Page 1160 has three distinct UI-generated Sub IDs, and Task #72 verified those links previously. Page 828 has contextual New/Used/Trade-in and app choices per task record. | Current page 828 source tags and GA4 outbound events not independently verified in this pass; page 1160 tags distinguish CTA classes, not necessarily a particular future ad campaign. No evidence of accepted actions by tag. | Page/asset choice where links are tagged; accepted-lead economics later and only when Impact report exposes matching tag. |
| A direct old @CarClever | Directory/direct URL and full Used/CPO product journey documented; old live app depends on Fractal. | Search or directory click may be observed at its origin, but platform connection, invocation and traffic-source retention through old app → Impact are not proved. Old app code inaccessible here. | App awareness/link intent; not an install count or attributable lead test. |
| Lite V2 standalone | `carclever-widget` feature branch root `app/layout.tsx` includes GA4 `G-HNN7CS0Z4L`; `app/carclever-lite-v2/page.tsx` has no `gtag` call; `components/v2/ResultCardV2.tsx` renders `link`/`fallbackLink` as plain anchors without click event. `app/api/chat-v2/route.ts` fixes the MCP endpoint server-side and instructs use of returned links unchanged. | GA4 actual receipt, same stream as WordPress, consent behavior, UTM persistence, useful chat/search/card events and per-source Impact Sub ID are unverified or absent from inspected client code. No client source is forwarded to the MCP in the inspected request. The live URL/deployment branch match is documented, not rechecked here. | Page visits if GA4 works; app searches/outbound by source cannot currently be claimed from this code. |
| R Reddit → site or app | Can measure ad impressions/clicks/spend at Reddit after launch; site/app destination exists. | Engagement and app-use events need verification. Supplied Edmunds §3.B forbids social-origin Edmunds traffic/commission without express written approval; an intermediate page does not fix that. | Founder-led GetCarWise/CarClever awareness and useful actions, with partner revenue excluded unless permission changes. |
| O/P organic or publisher → site/app | GSC cohorts and future publisher UTMs can distinguish acquisition at origin; page 1160's offer-specific Sub IDs already exist. | WordPress outbound and app handoff remain incomplete; partner property/source permission and any accepted-action association must be checked per offer. | Qualified site visits, then separately app interest and permitted approved actions. |
| D short partner handoff | Impact asset links and action reporting exist at account level. | Supplied Edmunds §3.A bars direct PPC→Edmunds and §3.B social→Edmunds. Other channels/offers require their own terms/property verification. | Only a specific permitted direct route can have approved-lead economics. |

## 3. Minimal gap closure, in order

1. **No code change for page 934's base Used URL.** It has already been read-only verified. After Sep 28/30 and separate page approval, create a *campaign-specific* Impact UI-generated Used link/Sub ID (not hand-written URL parameters) and replace only that CTA if the report will expose that Sub ID through pending/approved/reversed actions. Acceptance: inspected published href, Impact asset/destination, a separately authorized low-noise verification after pause, and a report field joining click/action. Rollback: restore recorded existing href. Do not change GSC cohort before Sep 28.
2. **Check collection/account mapping before engineering.** Read GA4 admin for the linked Search Console property, WordPress and Lite V2 stream IDs/hostnames, consent handling and actual page/event receipt from existing natural traffic. No test visit required before Sep 30. Acceptance: screenshots or exported configuration and a real non-owner cohort; if unavailable, mark unverified. Do not assume `G-HNN7CS0Z4L` is the WordPress stream.
3. **Lite V2 instrumentation only if a paid route needs it, after approval.** Add aggregate, consent-respecting `lite_search_submitted`, `lite_result_shown`, `lite_listing_outbound` with event source/route category and no VIN, ZIP or search free text; preserve standard campaign UTMs in the page URL and do not modify returned Impact URLs casually. The feature-branch files to review are `app/carclever-lite-v2/page.tsx` and `components/v2/ResultCardV2.tsx`. Acceptance: browser event/network inspection in a controlled post-pause test, GA4 report by source, correct unchanged destination; rollback previous deployment. This is **not** Task #79 Free-tier work.
4. **Old @CarClever route stays an awareness hypothesis** until its own Fractal source-to-app-use and app-to-Impact data can be inspected. Ask Fractal engineering only for a concrete source-preservation feasibility/implementation spec if an app paid campaign is prioritized. Do not spend to prove affiliate ROI through a chain with no demonstrated source marker. A directory click alone does not prove invocation.
5. **Use the existing page 1160 Sub IDs as a template, not proof of G1.** For page 828/New or Trade-in, inspect the actual published href and source-tag reports when that route becomes next priority. Avoid proliferating events or offer buttons in advance.

## 4. Gate and handoff

- **Sep 28:** preserve/review GSC cohort; no page edit until decision.
- **Sep 30:** cost/product gate, natural usage, Task #79 Free-tier status, GA4 property/host receipt, Impact action-report fields and approved cash/hours.
- **Before any G1 spend:** exact allowed Search copy/negative list, Keyword Planner CPC, consented GA4 paid landing, page 934 campaign-specific Impact Sub ID to action reporting and a total cap. If the Sub ID or accepted-action field is unavailable, position G1 as site-engagement learning or defer it; do not advertise a measurable approved-lead ROI test.
- **Separate approvals:** WordPress/production edits, analytics pixels or code deployment, account creation/billing, ads, publishing, outreach and subscriptions. Claude's Task #79 remains its own technical lane; this audit belongs to Task #78 marketing/analysis.

**Conclusion:** There is no demonstrated page-934 URL defect. The critical paid-economics gap is *source-specific approved-action attribution*, followed by confirmation that paid landing and meaningful app use are actually collected. The smallest immediate task is account/report verification from existing data, then one tagged page-934 CTA after the gates—not a broad engineering rebuild.
